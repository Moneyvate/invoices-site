= provide :title, 'Contact Types'

%table
  %thead
    %tr
      %th Name
      %th{colspan: 3} Actions
  - @contact_types.each do |contact_type|
    - @current_class = contact_type
    %tr
      %td= contact_type.name
      = render 'common/index_links'

%br

= link_to 'New Contact type', new_contact_type_path
