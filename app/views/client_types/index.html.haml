= provide :title, 'Client Types'

%table
	%thead
		%tr
			%th Name
			%th{colspan: 3} Actions
	- @client_types.each do |client_type|
		- @current_class = client_type
		%tr
			%td= client_type.name
			= render 'common/index_links'

%br

= link_to 'New Client Type', new_client_type_path
