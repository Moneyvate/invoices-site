- # Client
- if current_page? '/clients'
	%table
		%tr
			%th Name
			%th Type
			%th{colspan: 3} Actions
		- @clients.each do |client|
			%tr
				%td= client.name
				%td= client.client_type.name
				%td= link_to 'Show', client
				%td= link_to 'Edit', edit_client_path(client)
				%td= link_to 'Destroy', client, :method => :delete, :data => { confirm: 'Are you sure?' }
	%br
	= link_to 'New Client', new_client_path

- # Client Type
- if current_page? '/client_types'
	%table
		%tr
			%th Name
			%th{colspan: 3} Actions
		- @client_types.each do |client_type|
			%tr
				%td= client_type.name
				%td= link_to 'Show', client_type
				%td= link_to 'Edit', edit_client_type_path(client_type)
				%td= link_to 'Destroy', client_type, :method => :delete, :data => { confirm: 'Are you sure?' }
	%br
	= link_to 'New Client Type', new_client_type_path

- # Contact Type
- if current_page? '/contact_types'
	%table
		%tr
			%th Name
			%th{colspan: 3} Actions
		- @contact_types.each do |contact_type|
			%tr
				%td= contact_type.name
				%td= link_to 'Show', contact_type
				%td= link_to 'Edit', edit_contact_type_path(contact_type)
				%td= link_to 'Destroy', contact_type, :method => :delete, :data => { confirm: 'Are you sure?' }
